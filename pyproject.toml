[project]
name = "kamiskaze-tests"
version = "0.1.0"
description = "Playwright tests for Kamiskaze media player"
requires-python = ">=3.10"
dependencies = [
    "pytest>=8.0.0",
    "pytest-playwright>=0.5.0",
    "playwright>=1.40.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.uv]
dev-dependencies = []
